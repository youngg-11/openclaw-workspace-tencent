# OpenClaw 压缩优化提示词

## 会话压缩规则

请在我的会话中启用以下压缩规则：

1. **自动压缩触发条件**：
   - 会话超过 4000 tokens 时自动压缩
   - 或每 15 次对话后自动压缩

2. **压缩模式**：激进模式 (aggressive)
   - 保留关键决策和结论
   - 压缩掉冗余的讨论过程
   - 保留重要的文件路径、配置信息

3. **压缩前行为**：
   - 先将重要信息写入记忆文件
   - 生成会话摘要

4. **使用命令**：
   - `/compact` - 手动触发压缩
   - `/compact Focus on decisions` - 带提示的压缩

---

## 模型选择规则

日常对话用 MiniMax 2.5，编码任务用 DeepSeek V3.2，复杂推理用 Gemini。
